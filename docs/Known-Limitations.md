# Known Limitations

- Deployment platform credentials must be provided outside the repository.
- Managed PostgreSQL is required for production persistence.
- OCR quality depends on the uploaded document quality.
- Email delivery depends on SMTP configuration in the target environment.
- Browser-exposed frontend environment variables cannot store secrets.
